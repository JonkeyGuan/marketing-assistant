apiVersion: v1
kind: Service
metadata:
  name: customer-agent
  namespace: 0-marketing-assistant-demo
  labels:
    app: customer-agent
spec:
  selector:
    app: customer-agent
  ports:
  - name: http
    port: 8002
    targetPort: 8002
    protocol: TCP
  type: ClusterIP
