apiVersion: v1
kind: Service
metadata:
  name: coder-agent
  namespace: 0-marketing-assistant-demo
  labels:
    app: coder-agent
spec:
  selector:
    app: coder-agent
  ports:
  - name: http
    port: 8001
    targetPort: 8001
    protocol: TCP
  type: ClusterIP
