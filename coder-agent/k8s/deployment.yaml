apiVersion: apps/v1
kind: Deployment
metadata:
  name: coder-agent
  namespace: 0-marketing-assistant-demo
  labels:
    app: coder-agent
    app.kubernetes.io/name: coder-agent
    app.kubernetes.io/component: agent
spec:
  replicas: 1
  selector:
    matchLabels:
      app: coder-agent
  template:
    metadata:
      labels:
        app: coder-agent
    spec:
      containers:
      - name: coder-agent
        image: quay.io/jonkey/coder-agent:latest
        ports:
        - containerPort: 8001
          protocol: TCP
        envFrom:
        - configMapRef:
            name: coder-agent-config
        - secretRef:
            name: coder-agent-secrets
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /.well-known/agent.json
            port: 8001
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /.well-known/agent.json
            port: 8001
          initialDelaySeconds: 5
          periodSeconds: 5
