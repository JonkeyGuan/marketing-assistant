apiVersion: v1
kind: Service
metadata:
  name: marketing-agent
  namespace: 0-marketing-assistant-demo
  labels:
    app: marketing-agent
spec:
  selector:
    app: marketing-agent
  ports:
  - name: http
    port: 8003
    targetPort: 8003
    protocol: TCP
  type: ClusterIP
