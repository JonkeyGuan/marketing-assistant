apiVersion: v1
kind: Service
metadata:
  name: k8s-agent
  namespace: 0-marketing-assistant-demo
  labels:
    app: k8s-agent
spec:
  selector:
    app: k8s-agent
  ports:
  - name: http
    port: 8004
    targetPort: 8004
    protocol: TCP
  type: ClusterIP
